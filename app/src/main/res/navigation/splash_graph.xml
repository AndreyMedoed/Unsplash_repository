<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/splash_graph"
    app:startDestination="@id/splashFragment">

    <fragment
        android:id="@+id/splashFragment"
        android:name="com.example.unsplash.screens.splash.SplashFragment"
        android:label="Подготовка"
        tools:layout="@layout/fragment_splash" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.unsplash.screens.main.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" >
        <argument
            android:name="isShown"
            app:argType="boolean" />
        <argument
            android:name="isTokenNotOutdated"
            app:argType="boolean" />
    </activity>
</navigation>